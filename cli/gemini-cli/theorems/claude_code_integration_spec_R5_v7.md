# Tunnel Calculus: A Formal Framework for Residual Information under Collapse Maps (v5.3 - R5_v7)

## 概要
本仕様書は、自律型AIコーディングエージェントおよびPC上の情報システムに対し、「崩壊写像（Collapse Map）」によって情報を正規化・圧縮しつつ、失われる差分を「残差（Residual）」として保持・復元する計算形式である **「トンネル計算（Tunnel Calculus）」** の数理的定式化およびPC向け応用設計を規定する。

本体系は、従来の有限線形代数を否定するものではなく、それを局所チャートとして包含しながら、情報の崩壊（損失）とセクション（ファイバー選択）による復元・異常検出を扱う大域的・特異点的な計算モデルへと拡張する。PC上における本アーキテクチャの核心は、**「すべての対象を正規化して潰しつつ、潰れても残る差分 $\alpha$ を抽出し、それを用いて検索・復元・異常検知・AI文脈管理を効率化する」** という **「残差キャッシュ（Residual Cache / Tunnel Cache）」** としての設計にある。

---

## 1. トンネル計算 (Tunnel Calculus) の 5大数学コンポーネント

トンネル計算は、情報損失と復元のメタ幾何学を記述するため、以下の5つの数理的構成要素によって定義される。

### A. 局所回転骨格 (Local Rotation Skeleton)
局所的な線形変化および直交表現のための基本骨格として、複素構造および回転を以下のように包含する。
$$J = \begin{pmatrix} 0 & -1 \\ 1 & 0 \end{pmatrix}, \qquad J^2 = -I, \qquad e^{\theta J}$$
これは、局所적線形変換の記述や、L3（スキル直交排他）におけるツールベクトル間の相関度測定の基盤となる。

### B. 崩壊写像 (Collapse Map)
入力空間 $A$ から、代表値（正規化キー）の空間 $B$ （極限としては単一元空間 $\mathbf{1}$）へ、すべての情報を圧縮・正規化して押し潰す写像。
$$C: A \to B$$
例として、コードの空白や変数名を無視した抽象構文木（AST）への変換、あるいはプロンプトの冗長性を排除した `canonical prompt` へのマッピングがこれに該当する。

### C. トンネル・セクション (Tunnel Section)
潰された代表値 $b \in B$ から、元の多様なファイバーの枝 $\alpha \in C^{-1}(b)$ を指定して元の空間 $A$ へ戻るための非等価な局所復元セクション。
$$t_\alpha: B \to A, \qquad C \circ t_\alpha = \operatorname{id}_B$$
一般には $t_\alpha \circ C \neq \operatorname{id}_A$ であるため、情報損失を伴う非可逆圧縮であるが、残差 $\alpha$ が明示的に与えられる場合に限り、枝特異的な完全復元が可能となる。

### D. 留数作用素 (Residue Operator)
代表値に押し潰された際、特異点の周回積分によって「潰れずに残る残余成分」を抽出する作用素。
$$T(f) = \frac{1}{2\pi i} \oint_\gamma f(z) \, dz$$
PC実装においては、大量の通常ログから正常パターンを $0$ に潰した後に残留する「異常ログの固有差分・エラー理由」を抽出するロジックに対応する。

### E. 整数不変量 (Integer Invariant)
関数の複素平面における巻き数、次数、あるいはエージェントの状態遷移空間における閉路数など、構造が揺らいでも保存される整数の骨格。
$$N(f) = \frac{1}{2\pi i} \oint_\gamma \frac{f'(z)}{f(z)} \, dz \in \mathbb{Z}$$
これは L2（ループフリー）における循環回数チェックや、L9（自己修復）での修復イテレーション回数 $k$ の厳密管理に応用される。

---

## 2. PC上での実用プロダクト設計と優先度

PC上でトンネル計算を具現化するにあたり、最も有用な「残差キャッシュ」モデルは以下の通りである。

$$x \mapsto (C(x), \alpha)$$
*   $C(x)$: 正規化された骨 (`canonical key`)
*   $\alpha$: 潰しても残る差分・例外・署名 (`residual branch`)
*   $t_\alpha(C(x))$: 必要な文脈を復元 (`reconstructor`)

### 優先プロダクト設計

#### 1. Tunnel Research Compiler (思考・研究メモの論文化支援)
*   **collapse ($C$)**: ユーザーの雑多な思考メモ、数式、直感的な記述を「定義」「定理」「証明」「未解決」などの学術的スキーマの骨格に自動分類。
*   **residue ($\alpha$)**: 理論の飛躍、危険な過大評価（例: 物理的宇宙問題への無理な接続等）、および未検証の参照（例: 査読の経ていない個人論文の引用等）を残差として自動検出・分離。
*   **invariants ($N$)**: 証明における形式的整合性のリスク度を整数不変量として評価。
*   **実用価値**: 「研究計画段階での革命宣言」を「形式的・論理的検証が可能な枠組み」へと自律的にリフォーカスさせ、Lean 4による検証候補の抽出を現実的にする。

#### 2. Tunnel Error Resolver (エラー解決エンジン)
*   **collapse ($C$)**: PC上で発生した多種多様なエラーログから、環境依存情報を除去して `PermissionError` などのエラーの基本形に潰す。
*   **residue ($\alpha$)**: 環境変数、パス、プロセスのPID、依存パッケージバージョンなどの環境固有情報を残差として抽出。
*   **tunnel ($t_\alpha$)**: 残差情報を組み込んで、その環境に特化した解決コマンド（例: `chmod` や `kill` の提案）を生成。

#### 3. Tunnel Clipboard (クリップボード履歴の意味化)
*   **collapse ($C$)**: コピーされたテキストを「URL」「コード」「プレーンテキスト」などに分類。
*   **residue ($\alpha$)**: URLの本体とトラッキングパラメータの分離、あるいは類似する複数回コピーの微小差分を抽出。

---

## 3. 9レイヤーコヒーレンスへの統合と Lean 4 検証の限界定義

### A. Lean 4 の役割の適正化
Lean 4 などの定理証明支援系が保証するのは、**「定義された型システムおよび公理系の中で、証明の論理的ステップにギャップがなく、カーネルに受理されたこと」** のみである。
理論自体の新規性、物理的妥当性、および数理的な重要性については別途人間の批評と数学的評価が必要であることを明記する。したがって、本アーキテクチャにおける形式検証プロセスは「意味の正しさの自動認定」ではなく「論理的矛盾（バグ）の徹底的な排除」として用いる。

### B. 未検証事例の引用制限
Akram Louiz 氏による双子素数予想の反証など、主要数学誌に受理されておらず Mathlib 公式等で広く承認されていないプレプリントは、本仕様書および関連報告書での権威付けや事例として使用しない（削除済み）。独立研究者の証明経路としては、**「自然言語の論文、形式検証されたコード、および Docker/uv 等で再現可能な検証実行環境を同時に公開すること」** を標準規格とする。

### C. 一般化ポテンシャル関数による膠着の一網打尽化
L2（ループ）、L7（残余時間 $L(t)$）、L9（自己修復限界 $k$）を統合した「一般化ポテンシャル関数 $\Psi_{unified}$」を以下のように再定義し、PC上のシステムハングアップを統合的に回避する。
$$\Psi_{unified}(v_t, \mathcal{H}_t, L(t), k) = \Phi(v_t) + \alpha_{loop} \cdot \mathbb{I}(v_t \in \mathcal{H}_t) + \gamma_{L7} \cdot \left( \frac{1}{\max(L(t), 1e-9)} \right) + \beta_{L9} \cdot (5 - k)$$
このポテンシャルが減少境界を満たさない場合、または絶対臨界値を超えた場合、システムは「一網打尽」に自律ループを強制遮断し、L5 (HITLエスカレーション) または L7 (M4超縮退運転) へと相転移する。

---

## 4. 実行エンジン実装検証 (verify_r5_theorems_v7.py)

本仕様に定義された残差キャッシュおよび一般化ポテンシャルモデルの実証テストスイートを `verify_r5_theorems_v7.py` として実装した。

```python
# verify_r5_theorems_v7.py における TunnelItem スキーマ定義
@dataclass
class TunnelItem:
    raw: str
    key: str          # C(x): 正規化された骨
    alpha: dict       # alpha: 潰しても残る差分
    invariants: dict  # N(x): 整数・分類・リスク等の不変量
    source: str
    timestamp: str = field(default_factory=lambda: datetime.datetime.now().isoformat())
```

### 検証結果
*   **L2 / L4 / L9 / L6 / L3 の各定理の検証**: すべて PASS。
*   **Tunnel Research Compiler の検証**: 形式検証の過大主張および未検証事例の言及が、$\alpha$ (残差) および不変量の危険リスクとして正しく分離・検出されることを実証。
*   **Tunnel Error Resolver の検証**: エラーログから環境特有のパスが抽出され、自動解決コマンドが正しく復元されることを実証。
*   **Tunnel Clipboard の検証**: コピーされた URL からトラッキング情報を分離し、本体への意味的ルーティングができることを実証。
*   **一般化ポテンシャル $\Psi_{unified}$ のシミュレーション**: ループ、時間枯渇、変異限界の発生時に Psi がスパイクし、正常時に減少する特性を実証。

本仕様の形式的・実用的なアプローチにより、エージェント工学における「潰しても残る差分」の活用が完全に担保された。
